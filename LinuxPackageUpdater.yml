---
- hosts: all
  remote_user: ansible
  become: true
  become_method = 'sudo'
  
  tasks:
    - name: APT Package Updater
      apt:
        upgrade: yes
        update_cache: yes
    - name: Reboot the machine (Wait for 5 min)
      reboot:
        reboot_timeout: 300
