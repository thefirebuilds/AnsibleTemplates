---
- hosts: all
  become: true
  
  tasks:
    - name: APT Package Updater
      apt:
        upgrade: yes
        update_cache: yes
    - name: Reboot the machine (Wait for 5 min)
      reboot:
        reboot_timeout: 300
